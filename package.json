{
  "name": "zawsze-in-love",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:app": "node scripts/generate-app.mjs",
    "predev": "npm run generate:app",
    "dev": "vite",
    "prebuild": "npm run generate:app",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
